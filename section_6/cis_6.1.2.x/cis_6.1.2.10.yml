---

{{ if .Vars.deb13cis_level_2 }}
  {{ if .Vars.deb13cis_rule_6_1_2_10 }}
command:
  rsyslog_ca:
    depends-on:
    - package:rsyslog
    - package:rsyslog-gnutls
    title: 6.1.2.10 | Ensure rsyslog CA certificates are configured
    exec: grep -Psi -- 'DefaultNetstreamDriverCAFile' /etc/rsyslog.conf /etc/rsyslog.d/*.conf
    exit-status:
      or:
      - 0
      - 1
    stdout:
    - '.*:/global(DefaultNetstreamDriverCAFile="{{ .Vars.deb13cis_rsyslog_stream_cafile }}")'
    meta:
      server: 1
      workstation: 2
      CIS_ID:
      - 6.1.2.10
      NIST800-53R5:
      - AU-8
  {{ end }}
{{ end }}
