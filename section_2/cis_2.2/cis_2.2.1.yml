---

{{ if .Vars.deb13cis_level_1 }}
  {{ if not .Vars.deb13cis_nis_client_required }}
    {{ if .Vars.deb13cis_rule_2_2_1 }}
package:
  nis_client:
    title: 2.2.1 | Ensure nis client is not installed
    installed: false
    name: nis
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.2.1
      NIST800-53R5:
      - CM-11
    {{ end }}
  {{ end }}
{{ end }}
